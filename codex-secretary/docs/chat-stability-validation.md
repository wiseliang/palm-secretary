# 聊天稳定性修复与验证（2026-09-08）

本次针对用户列出的八项问题完成代码修复，已于 2026-09-08 通过 SSH 发布为 v0.16.1。

| 问题 | 修复 |
| --- | --- |
| 随机消息 ID | 历史按 turn/item 建立稳定键，旧格式使用确定性兼容键；临时消息归并保留 UI ID |
| 每 4 秒强制覆盖 | 健康流式期间不拉历史；后台对账改为 15 秒；历史按身份合并，保留快照缺失的已显示消息 |
| 完成后三次强刷 | 移除 250/1500/4500 ms 定时刷新，改为一次去重同步；失败由恢复对账重试 |
| REST/WS 争夺运行状态 | RunCoordinator 统一仲裁；拒绝过期 REST、缺失行判停、旧轮完成和已完成轮复活；REST 可恢复遗漏的终态事件 |
| 每个 delta 重绘历史 | 50 ms 合并增量；只替换目标消息对象；独立 memo 消息行 |
| 正文无 memo | 正文 memo；相同文件数据保留原引用 |
| App resume 事件风暴 | 200 ms 防抖、同步互斥、1 秒冷却；同会话历史请求共享进行中及短期完成结果 |
| 历史防回退不足 | 短文本与非前缀旧文本保护、最终全文收敛与封存、缺失历史保留与顺序恢复、导航和流式水位校验 |

新增行为测试：tests/message-sync-v027.mjs、tests/chat-coordinator-v028.mjs。覆盖稳定 ID、旧格式、临时消息归并、多个 item、短回复回退、最终全文校正、迟到增量、历史顺序、并发同步去重、失败重试、REST/WS 竞态及旧轮完成。更新旧测试中依赖具体轮询间隔、三次刷新和固定 effect 依赖的断言。

验证：完整 test:all 通过，仓库 check 通过（保留既有 img 警告），网页构建通过。全量 tsc 检查与 HEAD 基线分别为 27 条诊断，未新增诊断；这项额外检查本身仍不通过。

边界：未连接用户手机执行弱网/前后台/长对话真机验收，未测量真实设备帧率。本次使用客户端请求代次、流式水位和已完成消息封存保证显示不被旧数据覆盖；不包含服务端持久化事件序号、断线事件重放协议或客户端诊断平台。恢复依赖完整历史读取，不能将本次测试结果表述为这些额外能力已实现。

## 上线记录

- current：/opt/palm-secretary/releases/20260908-v0.16.1-chat-stability。
- 旧版本：/opt/palm-secretary/releases/20260908-v0.16.0-fd34914。
- 状态备份：/opt/palm-secretary/backups/20260908-v0.16.1-chat-stability/.palm。
- 源码归档 SHA256：ca2c8ee398be5e638fdd02aeee02c35d2c06bb7a2f3523543eaeca818307b73f。归档包含本次工作区改动，以哈希标识，不冒充已提交的 Git 版本。
- Linux 独立目录 check、test:all、build 通过；切换前后无活动/待核对任务，项目仍为 11 个。
- API/Web active、NRestarts=0；公网首页 200、health 正常、release.json 为 0.16.1、Service Worker 缓存 v12。
- 真实历史匹配、6 个模型读取、WebSocket、测试文件上传/预览/下载/删除成功，短期测试会话已撤销。近期服务错误日志计数为 0。
- 手机验收待用户执行：短回复结束后等待、长回复滚动阅读、后台恢复、断网恢复、会话快速切换、连续发送。
